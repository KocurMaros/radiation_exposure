# What to change

jetson_logs.sh, cube_loger - change saving to /mnt/log_usb if it is available, if not, then use the current setup

create systemd service that starts logging.

test robustness of connection loss

